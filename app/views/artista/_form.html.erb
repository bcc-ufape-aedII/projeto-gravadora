<%= form_with(model: artistum) do |form| %>
  <% if artistum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artistum.errors.count, "error") %> prohibited this artistum from being saved:</h2>

      <ul>
        <% artistum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome_artistico, style: "display: block" %>
    <%= form.text_field :nome_artistico %>
  </div>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div id="integrantes_fields">
    <%= form.fields_for :integrantes do |integrante_form| %>
      <%= render 'integrante_fields', f: integrante_form %>
    <% end %>
  </div>

  <div>
    <%= link_to_add_fields "Adicionar Integrante", form, :integrantes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
